#include <bits/stdc++.h>
using namespace std;
int fun(int arr[],int size){
    for(int i = 0 ; i < size ; i ++){
        arr[i]
    }

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    
    
    return 0;
}