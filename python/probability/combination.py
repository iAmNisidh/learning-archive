import math
import itertools
def main():
    total = math.comb(52,1)
    print(total)
    #another method 

    # cards = make_deck()
    # all_hands = itertools.combinations(cards,5)
    # for hand in all_hands:
    #     print(hand)

main()